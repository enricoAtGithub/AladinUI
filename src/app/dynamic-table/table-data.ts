export class TableData {
    tableName: string;
    configName: string;

    constructor(tableName: string, configName: string) {
        this.tableName = tableName;
        this. configName = configName;
    }
}
